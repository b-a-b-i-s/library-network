version: '2'

services:
  web:
    restart: on-failure
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    environment:
      - NODE_ENV=production
    volumes:
      - /code/node_modules
