version: '2'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    environment:
      - NODE_ENV=development
    ports:
      - "8000:8000"
    volumes:
      - ./:/code
      - /code/node_modules
    command: npm run debug
